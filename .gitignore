# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
build/
dist/

# 本地配置与密钥
config.yaml
.env

# 人机对话记录（个人内容，默认仅本地，供本地 AI 检索）
# 若想随仓库归档/让云端 AI 也能检索，删掉下面这行
conversations/

# 运行时状态（去重水位线等，M2）——不进 Git
data/_state/
*.sqlite
*.sqlite3

# 系统 / IDE
.DS_Store
.idea/
.vscode/

# 注意：data/<source>/ 下的 JSON 快照与 reports/ 是有意提交的（历史归档），不要忽略
